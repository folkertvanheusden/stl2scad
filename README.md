This script converts an .stl into a .scad script.
It only works with ascii stl files.

Before you can run it, enter:

```bash
numpy-stl
```

Then:

```bash
./s2s.py mystlfile.stl > output.scad
```



Folkert van Heusden
